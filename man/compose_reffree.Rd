% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reffree.R
\name{compose_reffree}
\alias{compose_reffree}
\title{Reference-free iterative doublet detection and composition}
\usage{
compose_reffree(
  query,
  hc_singlets = NULL,
  target_rate,
  n_rounds = 4L,
  resolution = 1,
  npcs = 30L,
  min_pair = 6L,
  sing_min = 15L,
  seed = 1L,
  cluster_fn = cluster_seurat,
  score_fn = score_phispace,
  norm_fn = function(sce) PhiSpace::scranTransf(sce)
)
}
\arguments{
\item{query}{A genes-by-cells matrix-like object of raw non-negative counts.}

\item{hc_singlets}{Optional initial non-missing logical singlet calls.}

\item{target_rate}{Expected doublet fraction, strictly between zero and one.}

\item{n_rounds}{Number of removal/reclustering updates after round zero.}

\item{resolution, npcs}{Clustering parameters passed to \code{cluster_fn}.}

\item{min_pair}{Minimum simulated cells required for a cluster-pair model.}

\item{sing_min}{Minimum cells required for a cluster singlet model.}

\item{seed}{Non-negative integer controlling clustering-time scoring, simulation,
and detector training.}

\item{cluster_fn}{Function returning one cluster label per supplied cell.}

\item{score_fn}{Advanced scoring function with the same interface as
\code{score_phispace()}.}

\item{norm_fn}{Function that normalizes the temporary reference object.}
}
\value{
An \code{obligato_reffree} object with per-cell composition, scores,
flags, final cluster membership, round diagnostics, and provenance.
}
\description{
Repeatedly clusters the currently unflagged cells, simulates cross-cluster
doublets, detects doublets, and removes them before the next clustering round.
Cluster pairs are proxies for cell-type pairs. This mode currently uses
PhiSpace score features only; the reference-based library-size detector is not
silently substituted because it has not been validated for iterative clusters.
}
